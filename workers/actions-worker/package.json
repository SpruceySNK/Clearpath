{
  "name": "actions-worker",
  "version": "0.1.0",
  "private": true,
  "description": "ClearPath actions worker — decision endpoints and D1 audit trail",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250320.0",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3",
    "wrangler": "^3.109.2"
  }
}
