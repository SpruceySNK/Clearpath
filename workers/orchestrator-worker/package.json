{
  "name": "orchestrator-worker",
  "version": "0.1.0",
  "private": true,
  "description": "ClearPath orchestrator — agentic AI decision pipeline for asset compliance analysis",
  "main": "dist/index.js",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250320.0",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.0",
    "wrangler": "^3.99.0"
  }
}
